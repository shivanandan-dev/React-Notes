- Type of props is `boolean` when not assigned any value and by default it is assigned to `true`.
    ```jsx
    function App(){
        <Component randomProp/> // randomProp: boolean = true
    }
    ```